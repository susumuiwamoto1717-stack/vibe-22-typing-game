# Code Typing Practice - プロジェクト情報

## 概要
e-typing (https://www.e-typing.ne.jp) をモデルにした、コーディング練習に特化したタイピング練習アプリ。
コーディングで頻出する記号（`[]{}()<>;:"'` など）の入力練習と、その読み方・使い分けも学べる。

## 技術スタック
- HTML/CSS/JavaScript のみ（フレームワークなし）
- `index.html` をブラウザで開くだけで動作
- ローカルテスト: `python3 -m http.server 8769` でサーバー起動 → `http://localhost:8769`

## ファイル構成

```
22_typing game/
├── index.html          # メインHTML（4画面: モード選択、待機、タイピング、結果）
├── CLAUDE.md           # このファイル
├── css/
│   └── style.css       # スタイル（e-typing風デザイン、白背景、オレンジアクセント）
├── js/
│   ├── app.js          # メインアプリロジック（状態管理、画面遷移、キー入力処理）
│   ├── keyboard.js     # キーボード表示・ハイライト（US/JIS対応）、指マッピング
│   ├── hands.js        # 手のSVGイラスト表示・指ハイライト
│   ├── symbols.js      # 記号ガイドデータ（英語名・日本語読み・用途説明）
│   └── snippets.js     # コードスニペットデータ（各モードの練習問題）
```

## 主要機能

### 練習モード（5種類）
- **記号特訓**: コーディング頻出記号を集中練習
- **HTML/CSS**: HTMLタグとCSSプロパティの練習
- **JavaScript**: JS構文・関数・配列操作
- **Ruby**: Ruby / Rails構文の練習
- **ミックス**: 全言語からランダム出題

### フロー（e-typing準拠）
1. モード選択画面
2. 待機画面（「スペースキーで開始」）
3. タイピング中（5問出題）
4. 結果画面（スコア、WPM、正確率、レベル判定、ミス文字一覧）

### タイピング画面の構成
- **ステータスバー**: タイマー、問題数、WPM、正確率
- **記号ガイド**: 現在の記号の英語名・日本語読み・用途を表示
- **ヒント**: コードの意味を日本語で解説
- **タイピングエリア**: ターミナル風（黒背景、白文字）
- **指ガイドテキスト**: 「左手 人差し指」のようなテキスト表示
- **キーボード**: 次に押すべきキーをオレンジでハイライト、Shiftも対応
  - 指ごとに色分け（ピンク=小指、オレンジ薄=薬指、緑=中指、青=人差し指、紫=親指）
- **手のイラスト**: キーボード下にSVGの左右の手、アクティブ指がオレンジハイライト

### キーボード対応
- US配列（デフォルト）
- JIS配列（ヘッダーのセレクトボックスで切り替え）

### スコア・レベル判定
- スコア = WPM × (正確率 / 100)
- レベル: Practice → Slow → OK → Good! → Fast → Professor → Ninja → Comet → Thunder

## デザイン方針
- e-typingを参考にした白背景、`#ff6600` オレンジがアクセントカラー
- タイピングエリアはターミナル風（`#1e1e1e` 黒背景）
- 入力済み文字: グレー(`#666`)、現在の文字: オレンジ下線、未入力: 白(`#e0e0e0`)、ミス: 赤

## 既知の技術的ポイント
- CSS優先度問題: 指の色分けクラスとアクティブハイライトの競合は `!important` で解決
- 正確率: `Math.floor` を使用（`Math.round` だとミスがあっても100%になる場合がある）
- 各スニペットの `hint` フィールドにコードの意味を日本語で詳細解説

## 今後の改善予定
- 手のイラストをもっとかわいいデザインにする
